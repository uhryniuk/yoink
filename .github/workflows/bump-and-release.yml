name: Bump & Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Version bump type"
        required: true
        type: choice
        options: [patch, minor, major]

jobs:
  bump-and-release:
    name: Bump version, tag, and release
    runs-on: ubuntu-latest
    permissions:
      contents: write  # push commit + tag, create GitHub release

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # full history so bump-my-version can tag correctly

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dev dependencies
        run: uv sync

      - name: Configure git
        run: |
          git config user.name "uhryniuk"
          git config user.email "duhry99@gmail.com"

      - name: Bump version
        run: uv run bump-my-version bump ${{ inputs.bump }}

      - name: Push commit and tag
        run: git push origin main --follow-tags

      - name: Read new version
        id: version
        run: echo "tag=$(git describe --tags --abbrev=0)" >> "$GITHUB_OUTPUT"

      - name: Create GitHub release
        run: |
          gh release create ${{ steps.version.outputs.tag }} \
            --title "${{ steps.version.outputs.tag }}" \
            --generate-notes \
            --verify-tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
